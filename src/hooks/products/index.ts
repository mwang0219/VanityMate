export { useProductList } from './useProductList';
export { useCategoryCount } from './useCategoryCount';

// 为了向后兼容，如果有其他地方还在使用 useProducts
export { useProductList as useProducts } from './useProductList'; 